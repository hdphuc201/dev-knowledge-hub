# Risks

## Active risks

| ID     | Severity | Description                                                                                                                                                                                 | Mitigation                                                                                                                                                                      |
| ------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-0001 | Medium   | Historical planning files appear deleted from the working tree, so prior context may be incomplete.                                                                                         | Use `docs/MASTER_PLAN.md` as the canonical source going forward and record decisions explicitly.                                                                                |
| R-0002 | Medium   | Repository bootstrap spans multiple toolchains and can drift without strict task boundaries.                                                                                                | Enforce one-task-per-turn execution via `docs/EXECUTION_STATE.md` and `docs/TASKS.md`.                                                                                          |
| R-0003 | Low      | Root workspace commands cannot execute real package tasks until `apps/*` and `packages/*` are scaffolded in later tasks.                                                                    | Keep current root commands as explicit no-ops with clear messaging, then replace no-op behavior with real task execution once packages exist.                                   |
| R-0005 | Medium   | Turbo execution is still unstable in the current Windows environment; a targeted `pnpm exec turbo run lint --filter @dev-knowledge-hub/web` reproduced exit code `-1073741515`.             | Keep `turbo.json` and Turbo-compatible package scripts in place, and revisit Turbo runtime validation in a later infrastructure-focused task if the environment issue persists. |
| R-0006 | Medium   | Docker Compose configuration now validates, but the local Docker Desktop engine was unavailable during `P00-007`, so the PostgreSQL container could not be started for a live health check. | Re-run `docker compose up -d postgres` and confirm the container reaches `healthy` once Docker Desktop is available in the local environment.                                   |
